services:
  app:
    build: .
    image: sknr/go-ws-reachability:latest
    # Uncomment the next lines in order to use your own config.json instead of the example one.
    # volumes:
    #   - ./data:/app/data
    restart: unless-stopped
